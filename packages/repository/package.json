{
  "name": "@zenith/repository",
  "version": "1.0.0",
  "description": "Drizzle data action functions",
  "license": "MIT",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "push": "yarn drizzle-kit generate && yarn drizzle-kit migrate",
    "dev": "ts-node -P tsconfig.json ./src/index.ts",
    "clean": "rm -rf dist .drizzle"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.3",
    "drizzle-kit": "^0.30.4",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.39.2",
    "drizzle-zod": "^0.7.0",
    "zod": "^3.24.1"
  }
}
